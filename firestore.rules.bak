rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ Local emulator only — DO NOT deploy to prod
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
