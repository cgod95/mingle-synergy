import { Link } from "react-router-dom";
import { getVenues } from "../lib/demoVenues";

export default function VenueList() {
  const venues = getVenues();
  return (
    <div className="mx-auto w-full max-w-5xl px-4 py-6">
      <h1 className="mb-4 text-2xl font-semibold">Venues</h1>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {venues.map(v => (
          <Link key={v.id} to={`/venues/${v.id}`} className="group overflow-hidden rounded-2xl border bg-white shadow-sm hover:shadow">
            <div className="relative aspect-[16/9] w-full overflow-hidden">
              <img
                src={v.thumb}
                alt={v.name}
                className="h-full w-full object-cover transition-transform duration-300 group-hover:scale-[1.03]"
                loading="lazy"
              />
              <div className="absolute inset-0 bg-gradient-to-t from-black/50 via-black/20 to-transparent" />
              <div className="absolute bottom-3 left-4 right-4 flex items-center justify-between">
                <div className="text-white">
                  <div className="text-lg font-semibold">{v.name}</div>
                  <div className="text-xs opacity-90">{v.address}</div>
                </div>
              </div>
            </div>
            <div className="p-4 text-sm text-neutral-600 line-clamp-2">{v.bio}</div>
          </Link>
        ))}
      </div>
    </div>
  );
}
