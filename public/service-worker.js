
// This is a simple service worker file that will be compiled by Workbox
// You don't need to edit this file manually as Workbox will inject its precache manifest

self.addEventListener('install', (event) => {
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  // Take control of all clients as soon as the service worker activates
  event.waitUntil(clients.claim());
});

// The rest will be auto-generated by Workbox during build
