
import { 
  mockUsers as users, 
  mockVenues as venues, 
  mockInterests as interests, 
  mockMatches as matches,
  getUsersAtVenue,
  hasMutualInterest,
  getUserMatches
} from './mock';

// Re-export for backward compatibility
export {
  users,
  venues,
  interests,
  matches,
  getUsersAtVenue,
  hasMutualInterest,
  getUserMatches
};
